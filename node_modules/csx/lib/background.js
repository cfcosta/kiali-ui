"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var inspect_1 = require("./utils/inspect");
var arrays_1 = require("./utils/arrays");
function background() {
    return arrays_1.map(arguments, function (background) {
        return [
            background.image,
            background.position,
            background.size,
            background.repeat,
            background.origin,
            background.clip,
            background.attachment,
            background.color
        ]
            .filter(inspect_1.isDefined)
            .join(' ');
    })
        .filter(inspect_1.isNotEmpty)
        .join(',');
}
exports.background = background;
