var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

import React from 'react';
import Label from './Label';
import { noop } from '../../common/helpers';

var DisposableLabel = function DisposableLabel(props) {
  return React.createElement(Label, props);
};

DisposableLabel.propTypes = _extends({}, Label.propTypes);

DisposableLabel.defaultProps = _extends({}, Label.defaultProps, {
  onRemoveClick: noop
});

export default DisposableLabel;